* show all drivers in all time :
  ```show all drivers```
* not end the conversation when the bot haven't responses:
  ```don't stop at the end of the conversation```
* end the conversation when the bot haven't responses:
  ```don't stop at the end of the conversation```
